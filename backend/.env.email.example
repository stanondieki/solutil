# =================================
# SOLUTIL SMTP EMAIL CONFIGURATION
# =================================

# Set to 'true' to enable real SMTP emails (even in development)
USE_REAL_SMTP=true

# SMTP Provider Configuration
# Options: gmail, outlook, yahoo, custom
SMTP_PROVIDER=gmail

# SMTP Credentials
# For Gmail: Use your Gmail address and App Password (not regular password)
# For others: Use appropriate credentials
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password

# Custom SMTP Settings (only needed if SMTP_PROVIDER=custom)
# SMTP_HOST=smtp.your-provider.com
# SMTP_PORT=587
# SMTP_SECURE=false

# =================================
# GMAIL SETUP INSTRUCTIONS
# =================================
# 1. Go to your Google Account settings
# 2. Navigate to Security > 2-Step Verification (enable if not already)
# 3. Go to Security > App passwords
# 4. Select app: Mail, Select device: Other (custom name)
# 5. Use the generated 16-character password in SMTP_PASS

# =================================
# OUTLOOK/HOTMAIL SETUP
# =================================
# 1. Go to account.live.com
# 2. Sign in and go to Security
# 3. Enable 2-step verification
# 4. Create app password for "Mail"
# 5. Use your email and app password

# =================================
# YAHOO SETUP
# =================================
# 1. Go to Yahoo Account Security
# 2. Enable 2-step verification
# 3. Generate app password
# 4. Use your Yahoo email and app password

# =================================
# CUSTOM SMTP SETUP
# =================================
# Set SMTP_PROVIDER=custom and configure:
# SMTP_HOST=your-smtp-server.com
# SMTP_PORT=587 (or 465 for SSL)
# SMTP_SECURE=false (true for port 465)
# SMTP_USER=your-username
# SMTP_PASS=your-password