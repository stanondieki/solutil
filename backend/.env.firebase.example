# Firebase Configuration
# Add these to your .env file in the backend directory

# Firebase Project Configuration
FIREBASE_PROJECT_ID=your-project-id
FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nYOUR_PRIVATE_KEY_HERE\n-----END PRIVATE KEY-----\n"
FIREBASE_CLIENT_EMAIL=firebase-adminsdk-xxxxx@your-project-id.iam.gserviceaccount.com

# Firebase Web App Configuration (from Firebase Console > Project Settings > General)
FIREBASE_API_KEY=your-api-key
FIREBASE_AUTH_DOMAIN=your-project-id.firebaseapp.com
FIREBASE_STORAGE_BUCKET=your-project-id.appspot.com
FIREBASE_MESSAGING_SENDER_ID=123456789012
FIREBASE_APP_ID=1:123456789012:web:abcdef123456

# Development Settings
NODE_ENV=development
USE_FIRESTORE_EMULATOR=false
FIRESTORE_EMULATOR_HOST=localhost:8080

# Database Selection (during migration period)
PRIMARY_DATABASE=mongodb
# Options: 'mongodb' | 'firestore'
# Start with 'mongodb', then switch to 'firestore' after migration

ENABLE_DUAL_WRITE=false
# Set to true when ready to start dual-write testing

# Migration Settings
MIGRATION_BATCH_SIZE=500
MIGRATION_CONTINUE_ON_ERROR=false

# =====================================
# SETUP INSTRUCTIONS:
# =====================================

# 1. Create Firebase Project:
#    - Go to https://console.firebase.google.com
#    - Create new project or select existing one
#    - Enable Firestore Database

# 2. Get Service Account Key:
#    - Go to Project Settings > Service Accounts
#    - Click "Generate new private key"
#    - Download the JSON file
#    - Extract the values for FIREBASE_PRIVATE_KEY and FIREBASE_CLIENT_EMAIL

# 3. Get Web App Config:
#    - Go to Project Settings > General
#    - Add a web app if you haven't already
#    - Copy the config values

# 4. Update Values:
#    - Replace all "your-project-id" with your actual Firebase project ID
#    - Replace FIREBASE_PRIVATE_KEY with the private_key from service account JSON
#    - Replace FIREBASE_CLIENT_EMAIL with the client_email from service account JSON
#    - Replace other config values with your actual Firebase web app config

# 5. Security:
#    - Keep this file secure and never commit to version control
#    - Add .env to your .gitignore file
#    - Use environment variables in production deployment